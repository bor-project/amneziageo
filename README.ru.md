# AmneziaGeo

[English](README.md) | **Русский**

![platform](https://img.shields.io/badge/platform-Windows-0078D6)
![engine](https://img.shields.io/badge/engine-AmneziaWG-2E7D32)
![license](https://img.shields.io/badge/license-GPL--3.0-blue)

AmneziaGeo работает на движке AmneziaWG. Это доработанный WireGuard, который труднее заблокировать. В туннель попадает только то, что вы выбрали сами, остальное идёт напрямую.

## Для чего

Обычный WireGuard принимает решение по IP-адресу и про домены ничего не знает. Если взять категорию сайтов и один раз развернуть её в список адресов, толку мало: у крупных CDN (например, Cloudflare) адреса постоянно меняются и у каждого поддомена свои. Приложение обращается к адресу, которого в списке не было, и трафик утекает мимо туннеля.

AmneziaGeo смотрит на реальные ответы DNS. Как только система узнаёт адрес выбранного вами домена, клиент видит этот адрес в ответе и сразу добавляет его в туннель, ещё до того как приложение откроет соединение. Целые страны задаются диапазонами адресов и добавляются в туннель прямо при подключении, потому что диапазоны стран не меняются.

## Что умеет

Два режима:

- **Полный туннель.** Всё через VPN, локальная сеть в обход. Есть аварийная блокировка (kill-switch): если туннель упал, трафик не уйдёт напрямую.
- **Выборочный туннель.** По умолчанию всё идёт напрямую. В туннель попадает только то, что подошло под ваши правила, а домены добавляются сами, как только для них приходит ответ DNS.

Правила задаются по домену, категории сайтов, стране, диапазону адресов или приложению. Из них собираются списки, которые можно подключать к разным конфигам.

**Отдельное приложение в туннель.** Добавьте программу (браузер, игру, мессенджер), и в туннель пойдёт только её трафик, куда бы она ни обращалась. Клиент следит за процессом и его дочерними процессами.

**Весь UDP в туннель.** Звонки и игры обычно узнают адреса своих серверов не через DNS, поэтому правило по домену их не ловит. Включаете тумблер, и весь исходящий UDP идёт в туннель, кроме локальной сети и самого VPN-сервера.

**Когда UDP заблокирован.** AmneziaWG работает поверх UDP, а в некоторых сетях (корпоративный и гостевой Wi-Fi, часть мобильных операторов) он не проходит. Тогда весь туннель можно пустить поверх WebSocket по TCP. Снаружи это выглядит как обычный HTTPS-трафик и проходит там, где UDP закрыт. Порт любой, задаётся в настройках. Если сервер требует авторизацию, укажите логин с паролем или токен.

Правки доменов и стран применяются на лету, без переподключения.

## Установка (Windows)

Нужны Windows 7, 10 или 11 (x64 или arm64) и права администратора: установщик ставит системную службу.

1. Скачайте установщик со страницы [Releases](../../releases). Есть сборки под x64 и arm64. Обычная сборка несёт в себе всё необходимое. Есть вариант поменьше, но для него нужен уже установленный [.NET 10 Desktop Runtime](https://dotnet.microsoft.com/download/dotnet/10.0).
2. Запустите. Установятся служба и приложение.
3. Откройте AmneziaGeo и импортируйте конфиг: файл `.conf`, QR-код или общий файл настроек.
4. Выберите список правил (или полный туннель) и подключитесь.

Дальше приложение обновляется само.

## Пример: Discord там, где режут UDP

1. Создайте список и назовите его `discord`.
2. В правилах приложений выберите запущенный Discord и добавьте.
3. Включите «Весь UDP в туннель»: голосовые серверы Discord приходят не через DNS.
4. Раз в сети режут UDP, в настройках транспорта включите WebSocket и укажите адрес своего сервера, порт и авторизацию (если сервер её требует).
5. Выберите список `discord` и подключитесь.

Текст и голос Discord идут через туннель, остальное напрямую.

## Сборка из исходников

Каждая платформа собирается отдельно. Сейчас поддерживается Windows.

### Windows

Понадобятся [.NET 10 SDK](https://dotnet.microsoft.com/download/dotnet/10.0), [WiX](https://wixtoolset.org) (для установщика) и git. Движок туннеля собирается из сабмодуля, поэтому начинать нужно с него.

#### 1. Забрать сабмодуль движка

Движок AmneziaWG для Windows (`amneziawg-windows`) подключён git-сабмодулем и в основной репозиторий не входит. Без него сборка движка сразу падает с понятной подсказкой. Заберите его:

```powershell
git submodule update --init --recursive
```

#### 2. Собрать движок туннеля

Чтобы туннель работал и на Windows 7, движок собирается версией Go с поддержкой Windows 7:

```powershell
# движок туннеля -> tunnel.dll
amneziageo-windows\tools\build-engine-win7.ps1
```

Первый запуск сам скачивает всё необходимое — Go, llvm-mingw и wintun через `build.cmd` сабмодуля, плюс отдельный Go для Windows 7, — проверяет загрузку по контрольной сумме и кладёт готовый `tunnel.dll` туда, откуда его берёт приложение. Параметры:

- `-Arch x64|x86|arm64` — целевая архитектура (по умолчанию `x64`).
- `-Upstream` — собрать стоковым Go, без поддержки Windows 7 (для сравнения).
- `-Force` — перекачать и пересобрать тулчейн.

#### 3. Собрать приложение и установщик

```powershell
# приложение и служба
dotnet build amneziageo-windows\AmneziaGeo.Windows.Ui\AmneziaGeo.Windows.Ui.csproj -c Release

# установщик -> dist\AmneziaGeo-<версия>-win-<арх>-<payload>.exe
amneziageo-windows\installer\AmneziaGeo.Windows.Installer.Bundle\build-installer.ps1
```

По умолчанию собирается один вариант: `x64`, framework-dependent (на целевой машине нужен установленный .NET 10 Desktop Runtime). Параметры сборщика (у каждого есть короткий алиас, `-h` — весь список):

- `-v, -Version N.N.N.N` — версия бандла и бинарей (иначе `0.0.1.<число коммитов>`).
- `-a, -Arch x64,arm64` — архитектуры списком или `all`.
- `-p, -Payload fdd,scd` — тип поставки: `fdd` — нужен установленный runtime (легче), `scd` — runtime внутри (ставится куда угодно, большой).
- `-c, -Configuration Debug|Release`.
- `-pre, -Prerelease` — вшить бета-канал обновлений.
- `-r, -Rebuild` — очистить перед сборкой.
- `-l, -ListOnly` — показать матрицу сборки и выйти.

arm64 собирается сквозным конвейером, но нативные части (`tunnel.dll`, `wintun.dll`, `wstunnel.exe`) пока только x64, поэтому рабочая цель сегодня — x64.

#### Запуск в отладке

Для разработки есть лаунчер: одной командой он поднимает бэкенд-агент и интерфейс в одном процессе. Запускайте из консоли «от имени администратора» (ставит службу и правила WFP, поднимает туннель); нужен уже собранный `tunnel.dll` из шага 2.

```powershell
# бэкенд + интерфейс в одном процессе
dotnet run --project amneziageo-windows\tools\AmneziaGeo.Windows.Launcher
```

Без флагов поднимаются обе части. Флаги: `--service` — только агент; `--ui` — только интерфейс; `--target <имя>` — сразу выбрать конфигурацию; `--config <путь.conf>` — зарегистрировать wg-quick конфиг и запуститься на нём.

## Linux

Linux-голова выходит двумя deb-пакетами из одного дерева исходников:

- `amneziageo` - агент, который работает службой systemd, движок AmneziaWG, которым он управляет, и
  консольный клиент `amneziageo` с полноэкранным интерфейсом. Пакет не тянет графических
  библиотек, поэтому годится серверу без рабочего стола.
- `amneziageo-gui` - графический интерфейс. Работает от пользователя рабочего стола и управляет тем
  же агентом через его контрольный сокет, поэтому требует `amneziageo` той же версии.

Серверу нужен только первый пакет, рабочему столу - оба. Всё, что настраивается в графическом
интерфейсе, доступно и из консольного клиента.

### Установка

Возьмите пакеты своей архитектуры со страницы [Releases](../../releases) - собираются amd64 и
arm64 - и поставьте через apt, он подтянет названные ими библиотеки:

```bash
# сервер
sudo apt install ./amneziageo_<версия>_amd64.deb

# рабочий стол
sudo apt install ./amneziageo_<версия>_amd64.deb ./amneziageo-gui_<версия>_amd64.deb
```

Агент сразу запускается и включается в автозагрузку. Бинарники лежат в `/usr/lib/amneziageo`,
библиотека - в `/var/lib/amneziageo`, клиент - `/usr/bin/amneziageo`, имя интерфейса агент берёт из
`/etc/default/amneziageo`. `apt remove` библиотеку оставляет, `apt purge` - удаляет.

### Сборка пакетов

```bash
git submodule update --init --recursive
amneziageo-linux/tools/build-deb.sh --arch amd64,arm64
```

Для сборки нужны .NET SDK и Go, целевой машине - ничего: пакеты self-contained. Результат кладётся
в `dist/`. Ключи: `--version N.N.N.N` (иначе `0.0.1.<число коммитов>`), `--arch amd64,arm64`,
`--out <каталог>`, `--no-gui`, `--debug`. Список альтернатив `libicu` в скрипте перечисляет
ICU-пакеты, которые могут оказаться в целевом дистрибутиве, - новый выпуск дистрибутива добавляет
туда одну запись.

Для машины не на Debian `amneziageo-linux/tools/install-server.sh` публикует агент и консольный
клиент из исходников прямо в `/opt/amneziageo`.

### Первый запуск

```bash
sudo amneziageo geo download
sudo amneziageo config import work --file work.conf     # либо --link 'vpn://…', либо --stdin
sudo amneziageo up work
sudo amneziageo settings set survive-reboot on
sudo amneziageo settings set periodic-reconnect-enabled on
```

`up <конфиг>` выбирает конфигурацию и подключается на ней, `select <конфиг>` только запоминает её
для следующего подключения. Список маршрутизации - одна настройка на всю машину, а не привязка к
конфигурации: `routing use <имя>` выбирает список, `routing use none` оставляет решение за
собственными `AllowedIPs` конфигурации.

`survive-reboot` поднимает туннель при старте агента, `periodic-reconnect-enabled` переподнимает
его, если туннель упал. Без них перезагрузка или падение движка оставляют сервер без туннеля.

### Повседневное

```bash
amneziageo status                  # что работает и что возьмёт следующее подключение
amneziageo doctor                  # проверки, на которых обычно спотыкается серверная установка
amneziageo --json config list      # вывод для скриптов
amneziageo log tail --level info
amneziageo tui                     # полноэкранная консоль по SSH
```

`amneziageo help` перечисляет все команды. Коды возврата: 0 - готово, 1 - агент отказал,
2 - ошибка в команде, 3 - агент недоступен, 5 - операция не реализована в Linux-агенте.

### Оговорки

- Агент работает от root: он создаёт устройство туннеля и правит маршруты, одного `CAP_NET_ADMIN`
  его проверке недостаточно.
- Управляющий сокет - `/tmp/CoreFxPipe_AmneziaGeo.Agent`, поэтому в юните нельзя включать
  `PrivateTmp`. Любая локальная учётная запись, дотянувшаяся до сокета, управляет агентом, в том
  числе читает ключи конфигураций.
- Linux-туннель применяет адреса, MTU и `AllowedIPs` самой конфигурации, выбранный список
  маршрутизации и резолверы, сохранённые для конфигурации. В режиме split туннель
  начинает с одного маршрута до резолвера, а дальше каждое назначение получает свой маршрут при
  первом обращении; `route-ttl-seconds` задаёт, сколько он живёт после последнего использования.
  Исключения, правила по приложениям и транспорт WebSocket сохраняются и показываются, но пока не
  применяются в Linux.
- Назначение выбирается по именам, которые запрашивает машина, поэтому приложение со своим
  резолвером поверх DoH решается только по адресу. Пока туннель не несёт IPv6, адрес по нему не
  выдаётся именам, которые правила отправляют в туннель, - иначе они ушли бы физическим путём.

## Консоль

Один набор команд на всех трёх платформах: общая сборка `AmneziaGeo.Cli`, а поверх неё тонкий хост
под каждую систему. Всё идёт через агента по протоколу `IpcContract`, базу консоль не трогает -
поэтому её правки сразу видят и агент, и интерфейс.

| платформа | чем запускать |
|---|---|
| Linux | `amneziageo <команда>` |
| Windows | `amneziageo.exe <команда>` из `%ProgramFiles%\AmneziaGeo` |
| Android | `adb shell am broadcast -a org.amneziageo.android.CLI -n org.amneziageo.android/.CliReceiver --es cmd "<команда>"` |

На Android ответ приходит в `data=` той же команды `adb`, целиком дублируется в logcat под тегом
`AmneziaGeoCli`, а с `--es out <путь>` пишется в файл. Аргументы с дефисами передавайте массивом:
`--esa args ops,--probe`. Приёмник закрыт разрешением `android.permission.DUMP`: оно есть у
`adb shell` и его не получает обычное приложение. Интерфейс поднимать не нужно - приёмник запускает
агента в своём процессе.

Что чаще всего нужно при отладке:

```bash
amneziageo status                   # состояние и таблица конфигураций
amneziageo --json status            # то же для скриптов
amneziageo doctor                   # проверки, на которых спотыкается установка
amneziageo runtime                  # конфигурация, которую возьмёт следующее подключение
amneziageo cache --filter youtube   # что агент разрешил и куда направил
amneziageo log tail --level info
amneziageo log say "начало прогона" # отметка в журнале агента
amneziageo ops                      # операции протокола и команды, которые их вызывают
amneziageo ops --probe              # какие операции реализует агент этой платформы
amneziageo ipc <операция> [арг...]  # вызвать любую операцию напрямую
```

`ops --probe` шлёт каждую операцию без аргументов: отказ от её обработчика значит, что операция
есть, а отказ диспетчера - что её нет. Операции, которые без аргументов сделали бы работу
по-настоящему (подключение, скачивание, удаление), помечаются `-` и не отправляются.

Коды возврата: 0 - готово, 1 - агент отказал, 2 - ошибка в команде, 3 - агент недоступен,
5 - операция не реализована агентом этой платформы.

## Лицензия

GPL-3.0 или новее, см. [LICENSE](LICENSE). Использует движок AmneziaWG под лицензией его авторов.

## Поддержать

Если AmneziaGeo оказался вам полезен и вы хотите поддержать его развитие, можно сделать донат в одной из криптосетей:

- Сеть TRON (TRC20): `TNHcrYqUv2pUfW7BEzYJyXfVk9wEJrs4FR`

Спасибо за поддержку!

## Благодарности

Построено на экосистеме [Amnezia VPN](https://github.com/amnezia-vpn): протокол и движки AmneziaWG. AmneziaGeo добавляет сверху выбор того, что идёт в туннель.
