<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- ============================================================
       AmneziaGeo look-and-feel, ported from the Avalonia Light theme.
       Interaction rule: NO hover recolour anywhere; the only feedback
       is a subtle press opacity (0.85) and a dimmed disabled state (0.5).
       ============================================================ -->

  <!-- ===== Colors ===== -->
  <Color x:Key="AgBg">#FFECEEF1</Color>
  <Color x:Key="AgPanel">#FFFFFFFF</Color>
  <Color x:Key="AgPanel2">#FFF7F8FA</Color>
  <Color x:Key="AgSidebar">#FFE7E9EC</Color>
  <Color x:Key="AgInset">#FFF0F2F5</Color>
  <Color x:Key="AgHover">#FFEEF0F3</Color>
  <Color x:Key="AgBorder">#FFE4E6EA</Color>
  <Color x:Key="AgBorder2">#FFD2D6DD</Color>
  <Color x:Key="AgFg">#FF181B20</Color>
  <Color x:Key="AgMuted">#FF646C78</Color>
  <Color x:Key="AgMuted2">#FF9AA1AB</Color>
  <Color x:Key="AgAccent">#FF2A6FDB</Color>
  <Color x:Key="AgAccentSoft">#1A2A6FDB</Color>
  <Color x:Key="AgUp">#FF1F9D57</Color>
  <Color x:Key="AgDown">#FFE0533D</Color>
  <Color x:Key="AgWarn">#FFCF9A17</Color>
  <Color x:Key="AgDangerText">#FFD64545</Color>
  <Color x:Key="AgDangerBorder">#FFE0A0A0</Color>
  <Color x:Key="AgOnAccent">#FFFFFFFF</Color>

  <!-- ===== Brushes ===== -->
  <SolidColorBrush x:Key="BackgroundBrush"   Color="{StaticResource AgBg}" />
  <SolidColorBrush x:Key="SurfaceBrush"      Color="{StaticResource AgPanel}" />
  <SolidColorBrush x:Key="Surface2Brush"     Color="{StaticResource AgPanel2}" />
  <SolidColorBrush x:Key="SidebarBrush"      Color="{StaticResource AgSidebar}" />
  <SolidColorBrush x:Key="InsetBrush"        Color="{StaticResource AgInset}" />
  <SolidColorBrush x:Key="HoverBrush"        Color="{StaticResource AgHover}" />
  <SolidColorBrush x:Key="BorderSoftBrush"   Color="{StaticResource AgBorder}" />
  <SolidColorBrush x:Key="Border2Brush"      Color="{StaticResource AgBorder2}" />
  <SolidColorBrush x:Key="TextPrimaryBrush"  Color="{StaticResource AgFg}" />
  <SolidColorBrush x:Key="TextMutedBrush"    Color="{StaticResource AgMuted}" />
  <SolidColorBrush x:Key="TextMuted2Brush"   Color="{StaticResource AgMuted2}" />
  <SolidColorBrush x:Key="AccentBrush"       Color="{StaticResource AgAccent}" />
  <SolidColorBrush x:Key="AccentSoftBrush"   Color="{StaticResource AgAccentSoft}" />
  <SolidColorBrush x:Key="SuccessBrush"      Color="{StaticResource AgUp}" />
  <SolidColorBrush x:Key="DangerBrush"       Color="{StaticResource AgDown}" />
  <SolidColorBrush x:Key="WarnBrush"         Color="{StaticResource AgWarn}" />
  <SolidColorBrush x:Key="DangerTextBrush"   Color="{StaticResource AgDangerText}" />
  <SolidColorBrush x:Key="DangerBorderBrush" Color="{StaticResource AgDangerBorder}" />
  <SolidColorBrush x:Key="OnAccentBrush"     Color="{StaticResource AgOnAccent}" />

  <!-- ===== Typography ===== -->
  <FontFamily x:Key="AppFontFamily">IBM Plex Sans, Segoe UI</FontFamily>
  <FontFamily x:Key="AppMonoFontFamily">IBM Plex Mono, Consolas, Courier New</FontFamily>

  <!-- ===== Base TextBlock style ===== -->
  <Style TargetType="{x:Type TextBlock}">
    <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}" />
    <Setter Property="FontSize"   Value="13" />
    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    <Setter Property="TextOptions.TextFormattingMode" Value="Ideal" />
    <Setter Property="TextOptions.TextRenderingMode"  Value="ClearType" />
  </Style>

  <Style x:Key="MutedTextBlock" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">
    <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
    <Setter Property="FontSize"   Value="11.5" />
  </Style>

  <Style x:Key="SectionLabel" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">
    <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
    <Setter Property="FontSize"   Value="11" />
    <Setter Property="FontWeight" Value="SemiBold" />
  </Style>

  <!-- ============================================================
       Base "soft" (secondary) button. Template fully redefined so the
       system theme cannot inject hover chrome. No hover recolour;
       IsPressed -> Opacity 0.85; disabled -> 0.5.
       ============================================================ -->
  <Style x:Key="SoftButton" TargetType="{x:Type Button}">
    <Setter Property="FontFamily"  Value="{StaticResource AppFontFamily}" />
    <Setter Property="FontSize"    Value="12.5" />
    <Setter Property="FontWeight"  Value="Medium" />
    <Setter Property="Foreground"  Value="{StaticResource TextPrimaryBrush}" />
    <Setter Property="Background"  Value="{StaticResource InsetBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource Border2Brush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding"     Value="14,7" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="Cursor"      Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Button}">
          <Border x:Name="bd"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="7"
                  SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="{TemplateBinding Padding}"
                              RecognizesAccessKey="True" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="bd" Property="Opacity" Value="0.85" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="bd" Property="Opacity" Value="0.5" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="{x:Type Button}" BasedOn="{StaticResource SoftButton}" />

  <!-- Accent / primary button -->
  <Style x:Key="AccentButton" TargetType="{x:Type Button}">
    <Setter Property="FontFamily"  Value="{StaticResource AppFontFamily}" />
    <Setter Property="FontSize"    Value="13" />
    <Setter Property="FontWeight"  Value="SemiBold" />
    <Setter Property="Foreground"  Value="{StaticResource OnAccentBrush}" />
    <Setter Property="Background"  Value="{StaticResource AccentBrush}" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Padding"     Value="18,8" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="Cursor"      Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Button}">
          <Border x:Name="bd"
                  Background="{TemplateBinding Background}"
                  CornerRadius="9"
                  SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="{TemplateBinding Padding}"
                              RecognizesAccessKey="True" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="bd" Property="Opacity" Value="0.85" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="bd" Property="Opacity" Value="0.5" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Destructive / "danger" outlined button -->
  <Style x:Key="DangerButton" TargetType="{x:Type Button}">
    <Setter Property="FontFamily"  Value="{StaticResource AppFontFamily}" />
    <Setter Property="FontSize"    Value="12.5" />
    <Setter Property="FontWeight"  Value="Medium" />
    <Setter Property="Foreground"  Value="{StaticResource DangerTextBrush}" />
    <Setter Property="Background"  Value="Transparent" />
    <Setter Property="BorderBrush" Value="{StaticResource DangerBorderBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding"     Value="14,7" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="Cursor"      Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Button}">
          <Border x:Name="bd"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="7"
                  SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="{TemplateBinding Padding}"
                              RecognizesAccessKey="True" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="bd" Property="Opacity" Value="0.85" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="bd" Property="Opacity" Value="0.5" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Card surface helper -->
  <Style x:Key="Card" TargetType="{x:Type Border}">
    <Setter Property="Background"  Value="{StaticResource SurfaceBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource BorderSoftBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="14" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
  </Style>

  <!-- Flat progress bar matching the accent -->
  <Style TargetType="{x:Type ProgressBar}">
    <Setter Property="Height" Value="6" />
    <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
    <Setter Property="Background" Value="{StaticResource InsetBrush}" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ProgressBar}">
          <Border Background="{TemplateBinding Background}" CornerRadius="3" SnapsToDevicePixels="True">
            <Border x:Name="PART_Track">
              <Border x:Name="PART_Indicator" HorizontalAlignment="Left"
                      Background="{TemplateBinding Foreground}" CornerRadius="3" />
            </Border>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
