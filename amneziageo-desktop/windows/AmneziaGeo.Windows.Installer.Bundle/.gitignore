# Build outputs + staged publishes + the generated payload group — all regenerated by build-installer.ps1.
bin/
obj/
obj-seed/
stage/
ba-publish/
dist/
BaPayloads.generated.wxs
*.wixpdb

# Local-only deploy tooling for the private update channel — kept out of VCS.
deploy.ps1
publish-nginx.ps1

# Config overlays (installer.config.local.json / installer.config.<env>.json) — secrets + per-env values.
# The base installer.config.json stays tracked as the template.
installer.config.*.json
